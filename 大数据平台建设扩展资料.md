# 一、 Hadoop安装

## 1. 获取Hadoop

Hadoop下载地址：[https://archive.apache.org/dist/hadoop/common/hadoop-3.1.3/](https://archive.apache.org/dist/hadoop/common/hadoop-2.7.2/)

或者在C盘0mylinux文件夹《开源组件》文件夹中

## 2.上传并解压

用XShell文件传输工具将hadoop-3.1.3.tar.gz导入到opt目录下面的software文件夹下面

![img](images/大数据平台建设扩展资料/wpse14c.tmp.jpeg)

 操作代码：

```shell
#进入到Hadoop安装包路径下
[bigdata@bigdatam10 software]$ cd /opt/software/
#解压到/opt/module
[bigdata@bigdatam10 software]$ tar -zxvf hadoop-3.1.3.tar.gz -C /opt/module/
#查看是否解压成功
[bigdata@bigdatam10 software]$ cd /opt/module/
[bigdata@bigdatam10 module]$ ll
#运行结果
total 0
drwxr-xr-x. 9 bigdata bigdata 149 Sep 11  2019 hadoop-3.1.3
drwxr-xr-x. 7 bigdata bigdata 245 Apr  1  2019 jdk1.8.0_212
```

## 3.配置环境变量

将Hadoop添加到环境变量

```shell
#进入hadoop-3.1.3
[bigdata@bigdatam10 module]$ cd hadoop-3.1.3/
#打印地址,并复制
[bigdata@bigdatam10 hadoop-3.1.3]$ pwd
/opt/module/hadoop-3.1.3
#打开自定义的环境变量配置文件,地址在：/etc/profile.d/my_env.sh
[bigdata@bigdatam10 hadoop-3.1.3]$ sudo vim /etc/profile.d/my_env.sh
```

 在my_env.sh文件末尾添加如下内容：（shift+i）

```shell
#HADOOP_HOME 
export HADOOP_HOME=/opt/module/hadoop-3.1.3
export PATH=$PATH:$HADOOP_HOME/bin 
export PATH=$PATH:$HADOOP_HOME/sbin
```

保存并退出：

```shell
 :wq
```

让修改后的文件生效

```shell
[bigdata@bigdatam10 hadoop-3.1.3]$ source /etc/profile
```

测试是否安装成功

```shell
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop version
```

![image-20230218150613367](imges/image-20230218150613367.png)

重启（如果Hadoop**命令不能用再重启**虚拟机）

```shell
[bigdata@bigdatam10 hadoop-3.1.3]$ sudo reboot
```

## 4 Hadoop目录结构

4.1  查看Hadoop目录结构

```shell
[bigdata@bigdatam10 hadoop-3.1.3]$ ll
```

![image-20230218150917959](imges/image-20230218150917959.png)

4.2  重要目录

（1）bin目录：存放对Hadoop相关服务（hdfs，yarn，mapred）进行操作的脚本

（2）etc目录：Hadoop的配置文件目录，存放Hadoop的配置文件

（3）lib目录：存放Hadoop的本地库（对数据进行压缩解压缩功能）

（4）sbin目录：存放启动或停止Hadoop相关服务的脚本

（5）share目录：存放Hadoop的依赖jar包、文档、和官方案例

## 5  Hadoop运行模式

5.1 Hadoop官方网站：http://hadoop.apache.org/

5.2 Hadoop运行模式包括：**本地模式**、**伪分布式模式**以及**完全分布式模式**。

**本地模式**：单机运行，只是用来演示一下官方案例。生产环境不用。数据存储在linux本地

**伪分布式模式：**也是单机运行，但是具备Hadoop集群的所有功能，一台服务器模拟一个分布式的环境。个别缺钱的公司用来测试，生产环境不用。数据存储在hdfs上

**完全分布式模式：**多台服务器组成分布式环境。生产环境使用。，数据存储在hdfs上，由多台服务器提供服务，在企业中大量使用，只要是真正做大数据的，基本上都是采用这种集群的方式



## 6  本地运行模式（官方WordCount）

6.1创建在hadoop-3.1.3文件下面创建一个wcinput文件夹

```shell
#创建文件夹
[bigdata@bigdatam10 hadoop-3.1.3]$ mkdir wcinput
#进入文件夹
[bigdata@bigdatam10 hadoop-3.1.3]$ cd wcinput/
#创建文本文档
[bigdata@bigdatam10 wcinput]$ vim word.txt
```

6.2 在文件中输入如下内容**【注意输入的单词是以空格作为间隔符的】**

```shell
hadoop yarn hadoop mapreduce bigdata bigdata
```

Ø 保存退出：:wq

6.3 执行程序

```shell
#回到Hadoop目录/opt/module/hadoop-3.1.3
[bigdata@bigdatam10 wcinput]$ cd /opt/module/hadoop-3.1.3/

#执行hadoop内置的wordcount程序
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop jar share/hadoop/mapreduce/hadoop-mapreduce-examples-3.1.3.jar wordcount wcinput wcoutput

#查看运行结果
[bigdata@bigdatam10 hadoop-3.1.3]$ cat wcoutput/part-r-00000 

#运行结果
bigdata 2
hadoop  2
mapreduce       1
yarn    1
```



## 7 Hadoop完全分布式

### 7.1 思考

**假设我们配置好了centos集群，每个节点上都单独配置好了jdk和hadoop的环境变量，但是大家思考个问题，目前这几台机中的hadoop相互之间有关系吗？**

目前没关系，而且hadoop的组件有HDFS、YARN，那这些组件之间是怎样配置的呢，我这里有说明吗，目前都没有，那就要求我们对这三台机器做好对应的规划



### 7.2 集群部署规划

注意：

- NameNode和SecondaryNameNode不要安装在同一台服务器，原因，这两个组件都比较消耗内存，如果把两个放在同一个放在一起，会造成资源的紧张

- ResourceManager也很消耗内存，不要和NameNode、SecondaryNameNode配置在同一台机器上。减少机器的压力

如果说我的机器资源非常充足，想把这三个组件放在同一台机器上，当然也是没问题的，但是一般情况下，我们还是把耗资源的组件分开，本次集群节点部署组件如下：

|      | bigdatam10        | bigdatas20                  | bigdatas30                 |
| ---- | ----------------- | --------------------------- | -------------------------- |
| HDFS | NameNode DataNode | DataNode                    | SecondaryNameNode DataNode |
| YARN | NodeManager       | ResourceManager NodeManager | NodeManager                |

### 7.3 配置文件说明

Hadoop配置文件分两类：默认配置文件和自定义配置文件，只有用户想修改某一默认配置值时，才需要修改自定义配置文件，更改相应属性值。

（1）默认配置文件：

| 要获取的默认文件     | 文件存放在Hadoop的jar包中的位置                           |
| -------------------- | --------------------------------------------------------- |
| [core-default.xml]   | hadoop-common-3.1.3.jar/core-default.xml                  |
| [hdfs-default.xml]   | hadoop-hdfs-3.1.3.jar/hdfs-default.xml                    |
| [yarn-default.xml]   | hadoop-yarn-common-3.1.3.jar/yarn-default.xml             |
| [mapred-default.xml] | hadoop-mapreduce-client-core-3.1.3.jar/mapred-default.xml |

（2）自定义配置文件：

**core-site.xml、hdfs-site.xml、yarn-site.xml、mapred-site.xml**四个配置文件存放在$HADOOP_HOME/etc/hadoop这个路径上，用户可以根据项目需求重新进行修改配置。

（3）配置集群配置文件

（3.1）核心配置文件 core-site.xml

```shell
#进入Hadoop配置文件夹
[bigdata@bigdatam10 ~]$ cd $HADOOP_HOME/etc/hadoop
#打印地址，确认进入
[bigdata@bigdatam10 hadoop]$ pwd
/opt/module/hadoop-3.1.3/etc/hadoop
#编辑核心配置文件
[bigdata@bigdatam10 hadoop]$ vim core-site.xml 

```

文件内容如下：

```shell
<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
 
<configuration>
    <!-- 指定NameNode的地址，默认值是9000 -->
    <property>
        <name>fs.defaultFS</name>
        <value>hdfs://bigdatam10:9000</value>  
    </property>
 
    <!-- 指定hadoop数据的存储目录 -->
    <property>
        <name>hadoop.tmp.dir</name>
        <value>/opt/module/hadoop-3.1.3/data</value>
    </property>
 
    <!-- 配置HDFS网页登录使用的静态用户为bigdata -->
    <property>
        <name>hadoop.http.staticuser.user</name>
        <value>bigdata</value>
    </property>
</configuration>
```

（3.2）HDFS配置文件 hdfs-site.xml

```shell
[bigdata@bigdatam10 hadoop]$ vim hdfs-site.xml 
```

文件内容如下：

```shell
<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
 
<configuration>
    <!-- nn web端访问地址-->
    <property>
        <name>dfs.namenode.http-address</name>
        <value>bigdatam10:50070</value>
    </property>
   <!-- 2nn web端访问地址-->
    <property>
        <name>dfs.namenode.secondary.http-address</name>
        <value>bigdatas30:50090</value>
    </property>
</configuration>
```

（3.3）YARN配置文件  yarn-site.xml

```shell
[bigdata@bigdatam10 hadoop]$ vim yarn-site.xml
```

文件内容如下：

```shell
<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
 
<configuration>
    <!-- 指定MR走shuffle -->
    <property>
        <name>yarn.nodemanager.aux-services</name>
        <value>mapreduce_shuffle</value>
    </property>
 
    <!-- 指定ResourceManager的地址-->
    <property>
        <name>yarn.resourcemanager.hostname</name>
        <value>bigdatas20</value>
    </property>
 
    <!-- 环境变量的继承 -->
    <property>
        <name>yarn.nodemanager.env-whitelist</name>
   <value>JAVA_HOME,HADOOP_COMMON_HOME,HADOOP_HDFS_HOME,HADOOP_CONF_DIR,CLASSPATH_PREPEND_DISTCACHE,HADOOP_YARN_HOME,HADOOP_MAPRED_HOME</value>
    </property>
</configuration>
```

（3.4）MapReduce配置文件  mapred-site.xml

```shell
[bigdata@bigdatam10 hadoop]$ vim mapred-site.xml
```

文件内容如下：

```shell
<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
 
<configuration>
    <!-- 指定MapReduce程序运行在Yarn上 -->
    <property>
        <name>mapreduce.framework.name</name>
        <value>yarn</value>
    </property>
</configuration>
```

### 7.4 分发资料

(1)在集群上分发配置好的Hadoop

```shell
#分发整个Hadoop-3.1.3文件包
[bigdata@bigdatam10 hadoop]$ xsync /opt/module/hadoop-3.1.3/

#去s20和s30上查看文件分发情况**
[bigdata@bigdatas20 ~]$ cat /opt/module/hadoop-3.1.3/etc/hadoop/core-site.xml 
[bigdata@bigdatas30 ~]$ cat /opt/module/hadoop-3.1.3/etc/hadoop/core-site.xml
```



(2) 分发自定义环境变量配置文件my_env.sh

```shell
 #注意：my_env.sh是自定义的系统配置文件，只有root账号有操作权限，因此加上sudo
 #目前三台主机之间只配置了bigdata的免密登录，因此当使用sudo运行脚本的时候，需要分别输入三台主机的代码
 [bigdata@bigdatam10 hadoop]$ sudo xsync /etc/profile.d/my_env.sh 
```

![image-20230218182219471](imges/image-20230218182219471.png)

（3）使配置文件生效

```shell
# 在s20上使配置文件生效
[bigdata@bigdatas20 module]$ source /etc/profile
#查看hadoop版本
[bigdata@bigdatas20 module]$ hadoop version

# 在s30上使配置文件生效
[bigdata@bigdatas30 ~]$ source /etc/profile
#查看hadoop版本
[bigdata@bigdatas30 ~]$ hadoop version

#如出现下图效果，则代表成功
```

![image-20230218182849403](imges/image-20230218182849403.png)



### 7.5 运行Hadoop集群

**（1）配置workers**

**workers:机器上有几个节点，这里就配置几个**

```shell
[bigdata@bigdatam10 hadoop]$ vim /opt/module/hadoop-3.1.3/etc/hadoop/workers
```

在该文件中增加如下内容：

```shell
bigdatam10
bigdatas20
bigdatas30
```

注意：该文件中添加的内容结尾不允许有空格，文件中不允许有空行。他是获取单行数据，如果有空格的话，会把空格也当做名称中的字符，最终会找不到加了空格的名称，所以一定要避免后面的空格

也不允许有空行，原因同上

同步所有节点配置文件

```shell
[bigdata@bigdatam10 hadoop]$ xsync /opt/module/hadoop-3.1.3/etc
```

**（2）格式化集群**

**如果集群是第一次启动**，需要在bigdatam10节点格式化NameNode，

format相当于我们买了一块硬盘，装在电脑上要对其进行初始化，这里的format也是做初始化的工作。

（注意：格式化NameNode，会产生新的集群id，导致NameNode和DataNode的集群id不一致，集群找不到已往数据。如果集群在运行过程中报错，需要重新格式化NameNode的话，一定要先停止namenode和datanode进程，并且要删除所有机器的data和logs目录，然后再进行格式化。）

```shell
#创建hdfs数据存放文件夹 [之前在配置文件中约定的]
[bigdata@bigdatam10 hadoop-3.1.3]$ mkdir data
#格式化hdfs
[bigdata@bigdatam10 hadoop-3.1.3]$ hdfs namenode -format
```



**（3）启动HDFS**

```shell
#在m10上启动hdfs
[bigdata@bigdatam10 hadoop-3.1.3]$ sbin/start-dfs.sh
#在m10上查看进程
[bigdata@bigdatam10 hadoop-3.1.3]$ jps
10546 NameNode
10674 DataNode
11411 Jps
#在s20上查看进程
[bigdata@bigdatas20 hadoop-3.1.3]$ jps
10949 Jps
10488 DataNode
#在s30上查看进程
[bigdata@bigdatas30 hadoop-3.1.3]$ jps
11024 Jps
10551 SecondaryNameNode
10478 DataNode
```

**（4）在浏览器中访问hdfs**

```shell
http://192.168.18.10:50070/
```

**出现以下界面，并且数据节点数量为3，则访问成功**

![image-20230219151844398](imges/image-20230219151844398.png)

如果出现以下页面则查看防火墙是否已关闭

```shell
#查看防火墙开关状态
[bigdata@bigdatam10 hadoop-3.1.3]$ systemctl status firewalld.service
#关闭防火墙
[bigdata@bigdatam10 hadoop-3.1.3]$ sudo systemctl stop firewalld.service 
#关闭防火墙后，刷新浏览器
```

![image-20230219152025825](imges/image-20230219152025825.png)



**如果三台机器明明都已经启动了，但50070处只显示一台数据节点，有可能是多次格式化导致cluster_id不一致，**处理方法：

把三台机器下/opt/module/hadoop-3.1.3/data文件夹删除掉，在m10上重新格式化，并且后期如果使用xsync脚本同步的时候，注意别同步data文件夹

（5）**在配置了ResourceManager的节点（bigdatas20）**启动YARN

```shell
#启动资源管理组件yarn
[bigdata@bigdatas20 hadoop-3.1.3]$ sbin/start-yarn.sh 

#在m10上查看启动进程
[bigdata@bigdatam10 hadoop-3.1.3]$ jps
10546 NameNode
10674 DataNode
11494 NodeManager
11612 Jps

#在s20上查看启动进程
[bigdata@bigdatas20 hadoop-3.1.3]$ jps
12355 NodeManager
12900 Jps
11958 DataNode
12223 ResourceManager

#在s30上查看启动进程
[bigdata@bigdatas30 hadoop-3.1.3]$ jps
11139 NodeManager
11317 Jps
10551 SecondaryNameNode
10478 DataNode
```

**（6）Web端查看YARN的ResourceManager**

```shell
#在浏览器中访问以下地址
http://192.168.18.20:8088/
```

![image-20230219154613790](imges/image-20230219154613790.png)



说明：如果无法访问，请查看s20上防火墙是否有关闭



### 7.6 集群基本测试

（1）上传文件到集群

 上传小文件

```shell
#在hdfs根目录上上创建文件夹input
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop fs -mkdir /input

#在NameNode的web端，查看文件夹列表
```

![image-20230219162957654](imges/image-20230219162957654.png)

```shell
#将hadoop-3.1.3根目录下的wcinput下word.txt上传到hdfs的input文件夹
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop fs -put $HADOOP_HOME/wcinput/word.txt /input
#web端访问效果如下
```

![image-20230219163231084](imges/image-20230219163231084.png)

上传大文件

```shell
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop fs -put /opt/software/jdk-8u212-linux-x64.tar.gz /input
```

![image-20230219164122479](imges/image-20230219164122479.png)

![image-20230219164401598](imges\image-20230219164401598.png)

（2）上传文件后查看文件存放在什么位置

Ø 查看HDFS文件存储路径

```shell
[bigdata@bigdatam10 subdir0]$ pwd
/opt/module/hadoop-3.1.3/data/dfs/data/current/BP-1745180864-192.168.18.10-1676794163189/current/finalized/subdir0
```

Ø 查看HDFS在磁盘存储文件内容

```shell
[bigdata@bigdatam10 subdir0]$ cat blk_1073741825
```

![image-20230219164936740](imges/image-20230219164936740.png)



（3）拼接

```shell
[bigdata@bigdatam10 subdir0]$ ll
-rw-rw-r--. 1 bigdata bigdata 134217728 Feb 19 00:40 blk_1073741826
-rw-rw-r--. 1 bigdata bigdata   1048583 Feb 19 00:40 blk_1073741826_1002.meta
-rw-rw-r--. 1 bigdata bigdata  60795424 Feb 19 00:40 blk_1073741827
-rw-rw-r--. 1 bigdata bigdata    474975 Feb 19 00:40 blk_1073741827_1003.meta
```

```shell
#拼接数据块
[bigdata@bigdatam10 subdir0]$  cat blk_1073741826>>tmp.tar.gz
[bigdata@bigdatam10 subdir0]$  cat blk_1073741827>>tmp.tar.gz
#解压问价
[bigdata@bigdatam10 subdir0]$ tar -zxvf tmp.tar.gz
```

（4）下载上传的jdk

```shell
#将hdfs中/input文件夹下的jdk下载到hadoop-3.1.3的根目录
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop fs -get /input/jdk-8u212-linux-x64.tar.gz ./
```

![image-20230219170655105](imges/image-20230219170655105.png)

（5）执行wordcount程序

```shell
#执行Mapreduce程序，读取hdfs下/input文件夹中文本文档，输出结果保存在hdfs的output文件夹下
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop jar share/hadoop/mapreduce/hadoop-mapreduce-examples-3.1.3.jar  wordcount /input/word.txt  /output
```

![image-20230219172135270](imges/image-20230219172135270.png)

查看运行结果

```shell
[bigdata@bigdatam10 hadoop-3.1.3]$ hadoop fs -cat /output/part-r-00000
#运行结果如下：
2023-02-19 01:22:33,312 INFO sasl.SaslDataTransferClient: SASL encryption trust check: localHostTrusted = false, remoteHostTrusted = false
bigdata 2
hadoop  2
mapreduce       1
yarn    1
```



### 7.7 群起hadoop脚本

**（1）. 查看全局变量路径**

```shell
#查看全局变量
[bigdata@bigdatam10 hadoop-3.1.3]$ echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/home/bigdata/bin:/opt/module/jdk1.8.0_212/bin:/opt/module/hadoop-3.1.3/bin:/opt/module/hadoop-3.1.3/sbin:/root/bin:/home/bigdata/bin:/opt/module/jdk1.8.0_212/bin:/opt/module/hadoop-3.1.3/bin:/opt/module/hadoop-3.1.3/sbin
```

发现当前账号bigdata的home/bin目录可以使用

```shell
#进入当前账号bigdata下的bin目录
[bigdata@bigdatam10 hadoop-3.1.3]$ cd /home/bigdata/bin/
#打印当前目录
[bigdata@bigdatam10 bin]$ pwd
/home/bigdata/bin
```

**（2） 创建脚本文件 myhadoop.sh**

通过notepad++的NPPFTP远程连接m10,打开myhadoop.sh,输入如下代码：

```shell
#!/bin/bash
 
if [ $# -lt 1 ]
then
    echo "No Args Input..."
    exit ;
fi
 
case $1 in
"start")
        echo " =================== 启动 hadoop集群 ==================="
 
        echo " --------------- 启动 hdfs ---------------"
        ssh bigdatam10 "/opt/module/hadoop-2.8.5/sbin/start-dfs.sh"
        echo " --------------- 启动 yarn ---------------"
        ssh bigdatas20 "/opt/module/hadoop-2.8.5/sbin/start-yarn.sh"
;;
"stop")
        echo " =================== 关闭 hadoop集群 ==================="
 
        echo " --------------- 关闭 yarn ---------------"
        ssh bigdatas20"/opt/module/hadoop-2.8.5/sbin/stop-yarn.sh"
        echo " --------------- 关闭 hdfs ---------------"
        ssh bigdatam10 "/opt/module/hadoop-2.8.5/sbin/stop-dfs.sh"
;;
*)
    echo "Input Args Error..."
;;
esac
```

保存

```shell
#授权：赋予脚本执行权限
chmod +x myhadoop.sh

#验证格式,如果使用Notepad++远程工具编辑的话，先查看格式，如果有存在dos，则在保存之前设置一下文档的格式
vim myhadoop.sh
```

![img](imges/1.png)

```shell
#先按ESC键，输入冒号，然后输入下面的内容
set fileformat=unix
```

### 7.7 群起JPS脚本

（1）建脚本文件jpsall，用来查看启动的进程

```shell
[bigdata@bigdatam10 bin]$ pwd
/home/bigdata/bin
[bigdata@bigdatam10 bin]$ vim jpsall
```

将以下代码拷贝到jpsall文件中

```shell
#!/bin/bash
for host in bigdatam10 bigdatas20 bigdatas30
do
        echo =============== $host ===============
        ssh $host jps
done
```

（2）保存后

```shell
# 给jps赋权
[bigdata@bigdatam10 bin]$ chmod +x jpsall

#资料分发
[bigdata@bigdatam10 bin]$ xsync /home/bigdata/bin/jpsall 
```

![image-20230219174511175](imges/image-20230219174511175.png)

### 7.8 验证脚本

5.1 验证jpsall

hadoop集群在启动状态输入jpsall

![image-20230219174723233](imges/image-20230219174723233.png)

5.2 验证myhadoop.sh

**关闭hadoop集群**

```shell
[bigdata@bigdatam10 bin]$ myhadoop.sh stop
```

![image-20230219175445344](imges/image-20230219175445344.png)

**启动hadoop集群**

```shell
[bigdata@bigdatam10 bin]$ myhadoop.sh start
```

![image-20230219175743529](imges/image-20230219175743529.png)



# 二、 Zookeeper概述

## 1 Zookeeper简介

![img](imges/clipboard.png)

Zookeeper 是一个开源的分布式协调服务，目前由 Apache 进行维护。Zookeeper 可以用于实现分布式系统中常见的发布/订阅、负载均衡、命令服务、分布式协调/通知、集群管理、Master 选举、分布式锁和分布式队列等功能。

它具有以下特性：

- 顺序一致性：从一个客户端发起的事务请求，最终都会严格按照其发起顺序被应用到 Zookeeper 中；
- 原子性：所有事务请求的处理结果在整个集群中所有机器上都是一致的；不存在部分机器应用了该事务，而另一部分没有应用的情况；
- 单一视图：所有客户端看到的服务端数据模型都是一致的；
- 可靠性：一旦服务端成功应用了一个事务，则其引起的改变会一直保留，直到被另外一个事务所更改；
- 实时性：一旦一个事务被成功应用后，Zookeeper 可以保证客户端立即可以读取到这个事务变更后的最新状态的数据。

## 2、Zookeeper设计目标

Zookeeper 致力于为那些高吞吐的大型分布式系统提供一个高性能、高可用、且具有严格顺序访问控制能力的分布式协调服务。它具有以下四个目标：

### 2.1 目标一：简单的数据模型

Zookeeper 通过树形结构来存储数据，它由一系列被称为 ZNode 的数据节点组成，类似于常见的文件系统。不过和常见的文件系统不同，Zookeeper 将数据全量存储在内存中，以此来实现高吞吐，减少访问延迟。

![img](imges/3652e6a7067.jpeg)

### 2.2 目标二：构建集群

可以由一组 Zookeeper 服务构成 Zookeeper 集群，集群中每台机器都会单独在内存中维护自身的状态，并且每台机器之间都保持着通讯，只要集群中有半数机器能够正常工作，那么整个集群就可以正常提供服务。

![img](imges/3652e6a70673333.jpeg)

### 2.3 目标三：顺序访问

对于来自客户端的每个更新请求，Zookeeper 都会分配一个全局唯一的递增 ID，这个 ID 反映了所有事务请求的先后顺序。

### 2.4 目标四：高性能高可用

ZooKeeper 将数据存全量储在内存中以保持高性能，并通过服务集群来实现高可用，由于 Zookeeper 的所有更新和删除都是基于事务的，所以其在读多写少的应用场景中有着很高的性能表现。

## 3、核心概念

### 3.1 集群角色

Zookeeper 集群中的机器分为以下三种角色：

- Leader ：为客户端提供读写服务，并维护集群状态，它是由集群选举所产生的；
- Follower ：为客户端提供读写服务，并定期向 Leader 汇报自己的节点状态。同时也参与写操作“过半写成功”的策略和 Leader 的选举；
- Observer ：为客户端提供读写服务，并定期向 Leader 汇报自己的节点状态，但不参与写操作“过半写成功”的策略和 Leader 的选举，因此 Observer 可以在不影响写性能的情况下提升集群的读性能。

### 3.2 会话

Zookeeper 客户端通过 TCP 长连接连接到服务集群，会话 (Session) 从第一次连接开始就已经建立，之后通过心跳检测机制来保持有效的会话状态。通过这个连接，客户端可以发送请求并接收响应，同时也可以接收到 Watch 事件的通知。

关于会话中另外一个核心的概念是 sessionTimeOut(会话超时时间)，当由于网络故障或者客户端主动断开等原因，导致连接断开，此时只要在会话超时时间之内重新建立连接，则之前创建的会话依然有效。

### 3.3 数据节点

Zookeeper 数据模型是由一系列基本数据单元 Znode(数据节点) 组成的节点树，其中根节点为 /。每个节点上都会保存自己的数据和节点信息。Zookeeper 中节点可以分为两大类：

- 持久节点 ：节点一旦创建，除非被主动删除，否则一直存在；
- 临时节点 ：一旦创建该节点的客户端会话失效，则所有该客户端创建的临时节点都会被删除。

临时节点和持久节点都可以添加一个特殊的属性：SEQUENTIAL，代表该节点是否具有递增属性。如果指定该属性，那么在这个节点创建时，Zookeeper 会自动在其节点名称后面追加一个由父节点维护的递增数字。

### 3.4 节点信息

每个 ZNode 节点在存储数据的同时，都会维护一个叫做 Stat 的数据结构，里面存储了关于该节点的全部状态信息。如下：

| 状态属性       | 说明                                                         |
| -------------- | ------------------------------------------------------------ |
| czxid          | 数据节点创建时的事务 ID                                      |
| ctime          | 数据节点创建时的时间                                         |
| mzxid          | 数据节点最后一次更新时的事务 ID                              |
| mtime          | 数据节点最后一次更新时的时间                                 |
| pzxid          | 数据节点的子节点最后一次被修改时的事务 ID                    |
| cversion       | 子节点的更改次数                                             |
| version        | 节点数据的更改次数                                           |
| aversion       | 节点的 ACL 的更改次数                                        |
| ephemeralOwner | 如果节点是临时节点，则表示创建该节点的会话的 SessionID；如果节点是持久节点，则该属性值为 0 |
| dataLength     | 数据内容的长度                                               |
| numChildren    | 数据节点当前的子节点个数                                     |

### 3.5 Watcher

Zookeeper 中一个常用的功能是 Watcher(事件监听器)，它允许用户在指定节点上针对感兴趣的事件注册监听，当事件发生时，监听器会被触发，并将事件信息推送到客户端。该机制是 Zookeeper 实现分布式协调服务的重要特性。

### 3.6 ACL

Zookeeper 采用 ACL(Access Control Lists) 策略来进行权限控制，类似于 UNIX 文件系统的权限控制。它定义了如下五种权限：

- CREATE：允许创建子节点；
- READ：允许从节点获取数据并列出其子节点；
- WRITE：允许为节点设置数据；
- DELETE：允许删除子节点；
- ADMIN：允许为节点设置权限。

## 4、ZAB协议

### 4.1 ZAB协议与数据一致性

ZAB 协议是 Zookeeper 专门设计的一种支持崩溃恢复的原子广播协议。通过该协议，Zookeeper 基于主从模式的系统架构来保持集群中各个副本之间数据的一致性。具体如下：

Zookeeper 使用一个单一的主进程来接收并处理客户端的所有事务请求，并采用原子广播协议将数据状态的变更以事务 Proposal 的形式广播到所有的副本进程上去。如下图：

![img](imges/4732e6a7067.jpeg)

具体流程如下：

所有的事务请求必须由唯一的 Leader 服务来处理，Leader 服务将事务请求转换为事务 Proposal，并将该 Proposal 分发给集群中所有的 Follower 服务。如果有半数的 Follower 服务进行了正确的反馈，那么 Leader 就会再次向所有的 Follower 发出 Commit 消息，要求将前一个 Proposal 进行提交。

### 4.2 ZAB协议的内容

ZAB 协议包括两种基本的模式，分别是崩溃恢复和消息广播：

#### 1）崩溃恢复

当整个服务框架在启动过程中，或者当 Leader 服务器出现异常时，ZAB 协议就会进入恢复模式，通过过半选举机制产生新的 Leader，之后其他机器将从新的 Leader 上同步状态，当有过半机器完成状态同步后，就退出恢复模式，进入消息广播模式。

#### 2）消息广播

ZAB 协议的消息广播过程使用的是原子广播协议。在整个消息的广播过程中，Leader 服务器会每个事物请求生成对应的 Proposal，并为其分配一个全局唯一的递增的事务 ID(ZXID)，之后再对其进行广播。具体过程如下：

Leader 服务会为每一个 Follower 服务器分配一个单独的队列，然后将事务 Proposal 依次放入队列中，并根据 FIFO(先进先出) 的策略进行消息发送。Follower 服务在接收到 Proposal 后，会将其以事务日志的形式写入本地磁盘中，并在写入成功后反馈给 Leader 一个 Ack 响应。当 Leader 接收到超过半数 Follower 的 Ack 响应后，就会广播一个 Commit 消息给所有的 Follower 以通知其进行事务提交，之后 Leader 自身也会完成对事务的提交。而每一个 Follower 则在接收到 Commit 消息后，完成事务的提交。

![img](imges/73742e6a7067.png)

## 5、Zookeeper的典型应用场景

### 5.1 数据的发布/订阅

数据的发布/订阅系统，通常也用作配置中心。在分布式系统中，你可能有成千上万个服务节点，如果想要对所有服务的某项配置进行更改，由于数据节点过多，你不可逐台进行修改，而应该在设计时采用统一的配置中心。之后发布者只需要将新的配置发送到配置中心，所有服务节点即可自动下载并进行更新，从而实现配置的集中管理和动态更新。

Zookeeper 通过 Watcher 机制可以实现数据的发布和订阅。分布式系统的所有的服务节点可以对某个 ZNode 注册监听，之后只需要将新的配置写入该 ZNode，所有服务节点都会收到该事件。

### 5.2 命名服务

在分布式系统中，通常需要一个全局唯一的名字，如生成全局唯一的订单号等，Zookeeper 可以通过顺序节点的特性来生成全局唯一 ID，从而可以对分布式系统提供命名服务。

### 5.3 Master选举

分布式系统一个重要的模式就是主从模式 (Master/Salves)，Zookeeper 可以用于该模式下的 Matser 选举。可以让所有服务节点去竞争性地创建同一个 ZNode，由于 Zookeeper 不能有路径相同的 ZNode，必然只有一个服务节点能够创建成功，这样该服务节点就可以成为 Master 节点。

### 5.4 分布式锁

可以通过 Zookeeper 的临时节点和 Watcher 机制来实现分布式锁，这里以排它锁为例进行说明：

分布式系统的所有服务节点可以竞争性地去创建同一个临时 ZNode，由于 Zookeeper 不能有路径相同的 ZNode，必然只有一个服务节点能够创建成功，此时可以认为该节点获得了锁。其他没有获得锁的服务节点通过在该 ZNode 上注册监听，从而当锁释放时再去竞争获得锁。锁的释放情况有以下两种：

- 当正常执行完业务逻辑后，客户端主动将临时 ZNode 删除，此时锁被释放；
- 当获得锁的客户端发生宕机时，临时 ZNode 会被自动删除，此时认为锁已经释放。

当锁被释放后，其他服务节点则再次去竞争性地进行创建，但每次都只有一个服务节点能够获取到锁，这就是排他锁。

### 5.5 集群管理

Zookeeper 还能解决大多数分布式系统中的问题：

- 如可以通过创建临时节点来建立心跳检测机制。如果分布式系统的某个服务节点宕机了，则其持有的会话会超时，此时该临时节点会被删除，相应的监听事件就会被触发。
- 分布式系统的每个服务节点还可以将自己的节点状态写入临时节点，从而完成状态报告或节点工作进度汇报。
- 通过数据的订阅和发布功能，Zookeeper 还能对分布式系统进行模块的解耦和任务的调度。
- 通过监听机制，还能对分布式系统的服务节点进行动态上下线，从而实现服务的动态扩容。





# 三、Zookeeper 集群搭建

## 1、单机环境搭建

### 1.1 下载

下载对应版本 Zookeeper，当前下载的版本 3.6.3。

官方下载地址：https://archive.apache.org/dist/zookeeper/

```
wget https://archive.apache.org/dist/zookeeper/zookeeper-3.6.3/zookeeper-3.6.3.tar.gz
```

或使用：

在D盘bigdata/开源组件文件夹中有Zookeeper-3.6.3,通过Ftp工具上传到/opt/software后，解压到/opt/module路径下：

### 1.2 解压

```shell
#解压
cd /opt/software
tar -zxvf apache-zookeeper-3.6.3-bin.tar.gz -C /opt/module/
#改名
cd /opt/module/
mv apache-zookeeper-3.6.3-bin zookeeper-3.6.3
```



### 1.3 配置环境变量

```shell
sudo vim /etc/profile.d/my_env.sh
```

添加环境变量：

```shell
#ZOOKEEPER_HOME
export ZOOKEEPER_HOME=/opt/module/zookeeper-3.6.3
export PATH=$PATH:$ZOOKEEPER_HOME/bin

```

使得配置的环境变量生效：

```shell
source /etc/profile
```

### 1.4 修改配置

进入安装目录的 conf/ 目录下，拷贝配置样本并进行修改：

```shell
 cp zoo_sample.cfg  zoo.cfg
```

指定数据存储目录和日志文件目录（目录不用预先创建，程序会自动创建），修改后完整配置如下：

```shell
# The number of milliseconds of each tick
tickTime=2000
# The number of ticks that the initial 
# synchronization phase can take
initLimit=10
# The number of ticks that can pass between 
# sending a request and getting an acknowledgement
syncLimit=5
# the directory where the snapshot is stored.
# do not use /tmp for storage, /tmp here is just 
# example sakes.
dataDir=/opt/module/zookeeper-3.6.3/data
dataLogDir=/opt/module/zookeeper-3.6.3/log
# the port at which the clients will connect
clientPort=2181
# the maximum number of client connections.
# increase this if you need to handle more clients
#maxClientCnxns=60
#
# Be sure to read the maintenance section of the 
# administrator guide before turning on autopurge.
#
# http://zookeeper.apache.org/doc/current/zookeeperAdmin.html#sc_maintenance
#
# The number of snapshots to retain in dataDir
#autopurge.snapRetainCount=3
# Purge task interval in hours
# Set to "0" to disable auto purge feature
#autopurge.purgeInterval=1

## Metrics Providers
#
# https://prometheus.io Metrics Exporter
#metricsProvider.className=org.apache.zookeeper.metrics.prometheus.PrometheusMetricsProvider
#metricsProvider.httpPort=7000
#metricsProvider.exportJvmInfo=true


```



配置参数说明：

- tickTime：用于计算的基础时间单元。比如 session 超时：N*tickTime；
- initLimit：用于集群，允许从节点连接并同步到 master 节点的初始化连接时间，以 tickTime 的倍数来表示；
- syncLimit：用于集群， master 主节点与从节点之间发送消息，请求和应答时间长度（心跳机制）；
- dataDir：数据存储位置；
- dataLogDir：日志目录；
- clientPort：用于客户端连接的端口，默认 2181

### 1.5 启动

由于已经配置过环境变量，直接使用下面命令启动即可：

在/opt/module/zookeeper-3.6.3/bin

```
 ./zkServer.sh start
```

![image-20220420042653015](imges/image-20220420042653015.png)

1.6 验证

使用 JPS 验证进程是否已经启动，出现 QuorumPeerMain 则代表启动成功。

```shell
jps
```

![image-20220420042718818](imges/image-20220420042718818.png)

## 2、集群环境搭建

为保证集群高可用，Zookeeper 集群的节点数最好是奇数，最少有三个节点，所以这里演示搭建一个三个节点的集群。这里我使用三台主机进行搭建，主机名分别为 hadoopm10，hadoops20，hadoops30。

### 2.1 修改配置

解压一份 zookeeper 安装包，修改其配置文件 zoo.cfg，内容如下：

```shell
tickTime=2000
initLimit=10
syncLimit=5
dataDir=/opt/module/zookeeper-3.6.3/data/
dataLogDir=/opt/module/zookeeper-3.6.3/log/
clientPort=2181

# server.1 这个1是服务器的标识，可以是任意有效数字，标识这是第几个服务器节点，这个标识要写到dataDir目录下面myid文件里
# 指名集群间通讯端口和选举端口
server.1=bigdatam10:2287:3387
server.2=bigdatas20:2287:3387
server.3=bigdatas30:2287:3387
```



### 2.2 标识节点

分别在三台主机的 dataDir 目录下新建 myid 文件,并写入对应的节点标识。Zookeeper 集群通过 myid 文件识别集群节点，并通过上文配置的节点通信端口和选举端口来进行节点通信，选举出 Leader 节点。

进入到/opt/module/zookeeper-3.6.3/data

```shell
vim myid
```

在该文件中输入数字1



### 2.3 分发资料

使用xsync分发资料

```
xsync /opt/module/zookeeper-3.6.3/
```

### 2.4 在s20和s30上更改myid的值

进入/opt/module/zookeeper-3.6.3/data

```shell
vim myid
```

将s20的值更改为2，将s30的值更改为3

![image-20220420044230953](imges/image-20220420044230953.png)

![image-20220420044308134](imges/image-20220420044308134.png)



### 2.5 启动集群

分别在三台主机上的/opt/module/zookeeper-3.6.3/bin中，执行如下命令启动服务：

```shell
 ./zkServer.sh start
```

通过jps查看是否有Zookeeper的进程



问题：启动后不久，自动关闭，把data目录下，除了myid外的其他文件删除

![image-20220420045326025](imges/image-20220420045326025.png)

分发资料的时候，注意myid文件是否会被覆盖，如果被覆盖了，则需要更改myid的值



### 2.6 集群验证

启动后使用 zkServer.sh status 查看集群各个节点状态。

```shell
cd /opt/module/zookeeper-3.6.3/bin
./zkServer.sh status
```



### 2.7 群起脚本

在/home/bigdata/bin目录下创建zk.sh文件，内容如下

```shell
#! /bin/bash
case $1 in
"start")
	for host in bigdatam10 bigdatas20 bigdatas30
	do
		echo "==========start $host zookeeper============"
		ssh $host "/opt/module/zookeeper-3.6.3/bin/zkServer.sh start"
	done
;;

"stop")
	for host in bigdatam10 bigdatas20 bigdatas30
	do
		echo "==========stop $host zookeeper============"
		ssh $host "/opt/module/zookeeper-3.6.3/bin/zkServer.sh stop"
	done
;;

"status")
	for host in bigdatam10 bigdatas20 bigdatas30
	do
		echo "==========status $host zookeeper============"
		ssh $host "/opt/module/zookeeper-3.6.3/bin/zkServer.sh status"
	done
;;

esac

```

赋权：

```shell
chmod +x zk.sh
```

分发

```
xsync /home/bigdata/bin
```

启动Zookeeper·集群

```shell
zk.sh start
```

![image-20220420060237262](imges/image-20220420060237262.png)

查看三台机器上的进程

```
jpsall
```

![image-20220420060332484](imges/image-20220420060332484.png)

查看三台机器上Zookeeper的状态

```shell 
zk.sh status
```



![image-20220420060436519](imges/image-20220420060436519.png)



# 四、Hive概述

## 1 Hive概述

### 1.1 Hive简介

Hive 是一个架构在 Hadoop 之上的数据仓库基础工具，它可以处理结构化和半结构化数据，它使得查询和分析存储在 Hadoop 上的数据变得非常方便。

在没有 Hive 之前，处理数据必须开发复杂的 MapReduce 作业，但现在有了 Hive，你只要开发简单的 SQL 查询就可以达到 MapReduce 作业同样的查询功能。Hive 主要针对的是熟悉 SQL 的用户。Hive 使用的查询语言称为 HiveQL（HQL），它跟 SQL 很像。HiveQL 自动把类 SQL 语句转换成 MapReduce 作业。Hive 对 Hadoop 的复杂性简单化了，而且使用 Hive 并不需要你学习 Java 语言。

Hive 一般在终端执行，并且把 SQL 语句转换成一系列能在 Hadoop 集群执行作业。Apache Hive 可以让存储在 HDFS 的数据以表的方式呈现。

### 1.2 **Apache Hive 历史**

Hive 由 Facebook 的技术团队开发的，Apache Hive 是众多满足 Facebook 业务需求的技术之一。它非常受 Facebook 内部所有用户的欢迎。它可以开发各种数据相关的应用，并且运行在具有数百个用户的集群之上。Facebook 的 Apache Hadoop 集群存储超过 2PB 的原始数据，并且每天定期加载 15TB 的数据。现在 Hive 被很多大公司使用并完善，比如亚马逊，IBM，雅虎，Netflix 等等大公司。

### 1.3 **为什么使用 Apache Hive**

在 Apache Hive 实现之前，Facebook 已经面临很多挑战，比如随着数据的爆炸式增长，要处理这些数据变得非常困难。而传统的关系型数据库面对这样海量的数据可以说无能为力。Facebook 为了克服这个难题，开始尝试使用 MapReduce。但使用它需要具备 java 编程能力以及必须掌握 SQL，这使得该方案变得有些不切实际。而 Apache Hive 可以很好的解决 Facebook 当前面临的问题。

Apache Hive 避免开发人员给临时需求开发复杂的 Hadoop MapReduce 作业。因为 hive 提供了数据的摘要、分析和查询。Hive 具有比较好的扩展性和稳定性。由于 Hive 跟 SQL 语法上比较类似， 这对于 SQL 开发人员在学习和开发 Hive 时成本非常低，比较容易上手。Apache Hive 最重要的特性就是不会 Java，依然可以用好 Hive。

### 1.4 **Hive 架构**

![img](imges/134221206440.png)

由上图可知，Hadoop 和 MapReduce 是 Hive 架构的根基。

Hive 架构包括如下组件：

- CLI（command line interface）
- JDBC/ODBC
- Thrift Server
- WEB GUI
- Metastore
- Driver（Complier、Optimizer 和 Executor）

这些组件可以分为两大类：

### 1.5 **服务端组件**

- **Driver 组件**：该组件包括 Complier、Optimizer 和 Executor，它的作用是将 HiveQL 语句进行解析、编译优化，生成执行计划，然后调用底层的 MapReduce 计算框架。
- **Metastore 组件**：元数据服务组件，这个组件存储 Hive 的元数据，hive 的元数据存储在关系数据库里，hive 支持的关系数据库有 derby、mysql。元数据对于 hive 十分重要，因此 hive 支持把 metastore 服务独立出来，安装到远程的服务器集群里，从而解耦 hive 服务和 metastore 服务，保证 hive 运行的健壮性。
- **Thrift 服务**：thrift 是 facebook 开发的一个软件框架，它用来进行可扩展且跨语言的服务的开发，hive 集成了该服务，能让不同的编程语言调用 hive 的接口。

### 1.6 **客户端组件**

- **CLI**：command line interface，命令行接口。
- **Thrift 客户端**：上面的架构图里没有写上 Thrift 客户端，但是 hive 架构的许多客户端接口是建立在 thrift客户端之上，包括 JDBC 和 ODBC 接口。
- **WEB GUI**：hive 客户端提供了一种通过网页的方式访问 hive 所提供的服务。这个接口对应 hive 的 hwi 组件（hive web interface），使用前要启动 hwi 服务。

![img](imges/134501543078.png)

### 1.7 **Hive Shell**

shell 是我们和 Hive 交互的主要方式，我们可以在 Hive shell 执行 HiveQL 语句以及其他命令。Hive Shell 非常像 MySQL shell，它是 Hive 的命令行接口。HiveQL 跟 SQL 一样也是不区分大小写的。我们可以以两种方式执行 Hive Shell：非交互式模式和交互式模式：

- **非交互式模式 Hive**：Hive Shell 可以以非交互式模式执行。只要使用 -f 选项并指定包含 HQL 语句的文件的路径。如：hive -f my-script.sql
- **交互式模式 Hive**：所谓交互式模式，就是在 Hive Shell 直接执行命令，Hive 会在 shell 返回执行结果。只要在 Linux Shell 输入 hive 即可进去交互式模式 hive shell。

![img](imges\134649899392.png)

### 1.8 **Apache Hive 的特性**

Hive 有很多特性，然我们来一个个看一下：

- Hive 以简单的方式提供数据摘要，数据查询和数据分析能力。
- Hive 支持外部表，这使得它可以处理未存储在 HDFS 的数据。
- Apache Hive 非常适合 Hadoop 的底层接口需求。
- 支持数据分区
- Hive 有一个基于规则的优化器，负责优化逻辑计划。
- 可扩展性以及可伸缩性
- 使用 HiveQL 不需要深入掌握编程语言，只有掌握基本的 SQL 知识就行，使用门槛较低。
- 处理结构化数据。
- 使用 Hive 执行即时查询做数据分析。

### 1.9 **Apache Hive 局限性**

Hive 具有以下局限性：

- Hive 不支持实时查询和行级更新。
- 高延迟。
- 不适用于在线事务处理。

## 2 Hive 安装

### 2.1 资料上传

将D盘bigdata/开源组件中的hive压缩包上传到/opt/software中

![image-20220420061641314](imges/image-20220420061641314.png)



### 2.2 解压和改名

```shell
cd /opt/software
tar -zxvf apache-hive-2.3.9-bin.tar.gz -C /opt/module

cd /opt/module
mv apache-hive-2.3.9-bin/ hive-2.3.9
```

![image-20220420062004632](imges/image-20220420062004632.png)

### 2.4 系统环境变量配置

```shell
sudo vim /etc/profile.d/my_env.sh 
```

在文档中添加

```
#HIVE_HOME
export HIVE_HOME=/opt/module/hive-2.3.9
export PATH=$PATH:$HIVE_HOME/bin
```

source配置文件

```shell
source /etc/profile
```



### 2.5 初始化元数据库

```shell
cd /opt/module/hive-2.3.9/bin
schematool  -dbType derby -initSchema
```

问题日志包冲突

![image-20220420114238270](imges/image-20220420114238270.png)

2.6 解决日志Jar包冲突

![image-20220420114701520](imges/image-20220420114701520.png)

```shell
mv $HIVE_HOME/lib/log4j-slf4j-impl-2.6.2.jar $HIVE_HOME/lib/log4j-slf4j-impl-2.6.2.bak
```



### 2.6 启动hive

```shell
[root@bigdatam10 hive-2.3.9]# bin/hive
```

报错：

![image-20220420115355828](imges/image-20220420115355828.png)

原因：连不上hadoop

解决方案：启动hadoop集群后，再次启动hive

![image-20220420115632771](imges/image-20220420115632771.png)

### 2.7 使用hive

```shell
hive> show databases;
```

报错：

FAILED: SemanticException org.apache.hadoop.hive.ql.metadata.HiveException: java.lang.RuntimeException: Unable to instantiate org.apache.hadoop.hive.ql.metadata.SessionHiveMetaStoreClient

解决方法：

将hive-2.3.9文件夹下的metastore_db删除后，重新初始化一下数据库即可解决

![image-20220420120924427](imges/image-20220420120924427.png)



重新进入hive,  执行show databases命令后，效果如下：

![image-20220420121133140](imges/image-20220420121133140.png)

其他命令：

```shell
#显示所有的表
hive> show tables;
```

![image-20220420121838728](imges/image-20220420121838728.png)

```shell
#创建数据表
hive> create table test(id int);
```

![image-20220420121752895](imges/image-20220420121752895.png)



```shell
#插入数据
hive> insert into test values(1);
```

![image-20220420121505837](imges/image-20220420121505837.png)



```shell
#查询数据
hive> select * from test;
```

![image-20220420121654262](imges/image-20220420121654262.png)



### 2.8 监控日志信息

2.8.1 监控日志文件

```shell
cd /tmp/bigdata

tail -f hive.log
```



注意：**原来的窗口不要动**



2.8.2 打开另一个窗口，进入到hive中，执行一个查询命令

![image-20220420122905680](imges/image-20220420122905680.png)



2.8.3 打开日志监控窗口，会报如下错误：

![image-20220420122546683](imges/image-20220420122546683.png)



报错具体代码：

Caused by: ERROR XSDB6: Another instance of Derby may have already booted

the database /opt/module/hive/metastore_db.

at org.apache.derby.iapi.error.StandardException.newException(Unknown

Source)

at org.apache.derby.iapi.error.StandardException.newException(UnknownSource)

at org.apache.derby.impl.store.raw.data.BaseDataFileFactory.privGetJBMSLockO

nDB(Unknown Source)

at org.apache.derby.impl.store.raw.data.BaseDataFileFactory.run(Unknown

Source)

...

原因在于 Hive 默认使用的元数据库为 derby，开启 Hive 之后就会占用元数据库，且不与其他客户端共享数据，所以我们需要将 Hive 的元数据地址改为 MySQL。







## 3 更改hive的元数据库为mysql

### 3.1 将 MySQL 的 JDBC 驱动拷贝到 Hive 的 lib 目录下

```shell
 cp /opt/software/mysql-connector-java-5.1.49.jar $HIVE_HOME/lib
```

### 3.2  配置 Metastore 到 MySQL

3.2.1 创建hive-site.xml配置文件

位置：$HIVE_HOME/conf下，添加如下内容

```xml
<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<configuration>
    <!-- jdbc 连接的 URL -->
    <property>
        <name>javax.jdo.option.ConnectionURL</name>
        <value>jdbc:mysql://bigdatam10:3306/metastore?useSSL=false</value>
    </property>
    <!-- jdbc 连接的 Driver-->
    <property>
        <name>javax.jdo.option.ConnectionDriverName</name>
        <value>com.mysql.jdbc.Driver</value>
    </property>
    <!-- jdbc 连接的 username-->
    <property>
        <name>javax.jdo.option.ConnectionUserName</name>
        <value>root</value>
    </property>
    <!-- jdbc 连接的 password -->
    <property>
        <name>javax.jdo.option.ConnectionPassword</name>
        <value>000000</value>
    </property>
    <!-- Hive 元数据存储版本的验证 -->
    <property>
        <name>hive.metastore.schema.verification</name>
        <value>false</value>
    </property>
    <!--元数据存储授权-->
    <property>
        <name>hive.metastore.event.db.notification.api.auth</name>
        <value>false</value>
    </property>
    <!-- Hive 默认在 HDFS 的工作目录 -->
    <property>
        <name>hive.metastore.warehouse.dir</name>
        <value>/user/hive/warehouse</value>
    </property>
</configuration>
```

3.2.2 登录MySQL

```shell
mysql -uroot -p000000
```

3.2.3 创建Hive元数据库

```shell
mysql> create database metastore;
#退出
mysql> quit;
```

3.2.4 初始化Hive元数据库

```shell
bin/schematool -initSchema -dbType mysql -verbose
```

### 3.3 再次启动Hive

操作路径：/opt/module/hive-2.3.9

3.3.1 启动Hive

```shell
bin/hive
```

3.3.2使用Hive

```shell
hive> show databases;
hive> show tables;
hive> create table test (id int);
hive> insert into test values(1);
hive> select * from test;
```

3.3.3 在xshell中开启另一个窗口开启hive（重复上面的日志监控操作）

```shell
hive> show databases;
hive> show tables;
hive> select * from aa;
```

在执行insert语句时，如出现如下错误：

FAILED: Execution Error, return code 2 from org.apache.hadoop.hive.ql.exec.mr.

解决方法，在hive cli执行如下代码：

```shell
set hive.exec.mode.local.auto=true;
```





### 3.4 使用元数据服务的方式访问Hive

3.4.1 在hive-site.xml文件中添加如下配置信息

```shell
<!-- 指定存储元数据要连接的地址 -->
<property>
<name>hive.metastore.uris</name>
<value>thrift://bigdatam10:9083</value>
</property>
```

3.4.2 启动metastore

```shell
hive --service metastore
```

注意: 启动后窗口不能再操作，需打开一个新的 shell 窗口做别的操作

3.4.3 启动hive

```shell
 bin/hive
```



退出hadoop安全模式

```shell
hadoop dfsadmin -safemode leave
```





### 3.5 使用JDBC方式访问Hive

3.5.1 在hive-site.xml文件中添加如下配置信息

```xml
<!-- 指定 hiveserver2 连接的 host -->
<property>
    <name>hive.server2.thrift.bind.host</name>
    <value>bigdatam10</value>
</property>
<!-- 指定 hiveserver2 连接的端口号 -->
<property>
    <name>hive.server2.thrift.port</name>
    <value>10000</value>
</property>
```



3.5.2 启动hiveserver2

```shell
bin/hive --service hiveserver2
```

3.5.3 启动beeline客户端（需要多等待一会儿）

```shell
bin/beeline -u jdbc:hive2://bigdatam10:10000 -n bigdata
```

问题1：

```shell
Error: Could not open client transport with JDBC Uri: jdbc:hive2://bigdatam10:10000: Failed to open new session: java.lang.RuntimeException: org.apache.hadoop.ipc.RemoteException(org.apache.hadoop.security.authorize.AuthorizationException): User: bigdata is not allowed to impersonate anonymous (state=08S01,code=0)
```

问题1解决方法：

在hadoop的etc/hadoop/core-site.xml中添加如下内容：

```xml
<property>
    <name>hadoop.proxyuser.bigdata.hosts</name>
    <value>*</value>
</property>
<property>
    <name>hadoop.proxyuser.bigdata.groups</name>
    <value>*</value>
</property>
```

注意：使用什么账号登录，则proxyuser后面的字段就用什么



hdfs-site.xml中添加：

```xml
<property>
	<name>dfs.webhdfs.enabled</name>
    <value>true</value>
</property>
```

添加完成后重启hadoop



问题2：

```shell
[bigdata@bigdatam10 hive-2.3.9]$ bin/beeline -u jdbc:hive2://bigdatam10:10000 -n bigdata
Connecting to jdbc:hive2://bigdatam10:10000
22/04/26 11:19:38 [main]: WARN jdbc.HiveConnection: Failed to connect to bigdatam10:10000
Could not open connection to the HS2 server. Please check the server URI and if the URI is correct, then ask the administrator to check the server status.
Error: Could not open client transport with JDBC Uri: jdbc:hive2://bigdatam10:10000: java.net.ConnectException: Connection refused (Connection refused) (state=08S01,code=0)

```

排查方法：确保服务启动

```shell
#在一个窗口上打开
bin/hive --service metastore
#在另一个窗口打开
bin/hiveserver2
#在第三个窗口访问
bin/beeline -u jdbc:hive2://:10000 -n bigdata
```

进入beeline后访问方式：

```shell
登录beeline
!connect jdbc:hive2://node1:10000
username是root
password 直接回车即可
```

3.5.4 看到如下界面

```shell
Connecting to jdbc:hive2://bigdatam10:10000
Connected to: Apache Hive (version 2.3.9)
Driver: Hive JDBC (version 2.3.9)
Transaction isolation: TRANSACTION_REPEATABLE_READ
Beeline version 2.3.9 by Apache Hive
0: jdbc:hive2://bigdatam10:10000> 
```



3.5.5 执行插入操作时报错

```xml
User: bigdata is not allowed to impersonate anonymous
```

解决方法：

在hive-site.xml中添加：

```xml
 <property>
        <name>hive.server2.enable.doAs</name>
        <value>false</value>
</property>
```

hive.server2.enable.doAs设置成false则，yarn作业获取到的hiveserver2用户都为hive用户。



问题：

```shell
Error: org.apache.hive.service.cli.HiveSQLException: Error while processing statement: FAILED: Execution Error, return code 2 from org.apache.hadoop.hive.ql.exec.mr.MapRedTask
```

解决方法：

```shell
set hive.exec.mode.local.auto=true;
```



# 五、 Hive的使用

## 1 使用Hive的准备工作

### 1.1 启动hadoop集群

```shell
#在任意目录下执行myhadoop.sh命令
myhadoop.sh start
```

使用jpsall监控hadoop进程

![image-20220428164215358](imges/image-20220428164215358.png)

说明：如果使用高可用方案，还需要启动Zookeeper,当前使用单机版hive



### 1.2 启动mysql服务

```shell
#启动mysql服务
sudo service mysql start

#登录mysql,用户名root，密码000000
mysql -uroot -p000000
```

验证hive元数据库是否存在

![image-20220428164614804](imges/image-20220428164614804.png)

验证是否有初始化成功

```shell
#切换到元数据库
mysql> use metastore;
#列出metastore中的所有的表
mysql> show tables;

```

![image-20220428164758643](imges/image-20220428164758643.png)





## 2 启动Hive服务

### 2.1 启动hive的元数据服务

```shell
#在一个窗口上打开
bin/hive --service metastore
```

### 2.2 使用hiveCli客户端访问HIve

```shell
#进入Hive的命令行工具
bin/hive
#显示所有数据库
show databases;
```

![image-20220428165346536](imges\image-20220428165346536.png)



### 2.3 使用beeline客户端访问Hive

```shell
#开启hiveserver2服务
bin/hiveserver2
#使用beeline客户端通过jdbc的方式访问hive
bin/beeline -u jdbc:hive2://bigdatam10:10000 -n bigdata
```

![image-20220428170019117](imges\image-20220428170019117.png)



### 2.4 常见问题

#### 2.4.1 insert时报错

```shell
Error: org.apache.hive.service.cli.HiveSQLException: Error while processing statement: FAILED: Execution Error, return code 2 from org.apache.hadoop.hive.ql.exec.mr.MapRedTask
```

解决方法：

```shell
set hive.exec.mode.local.auto=true;
```





# 六、生产数据

## 1.相关脚本 

说明：数据生产脚本放置在  /opt/openresty/bin 目录下

### 1.1 Nginx服务脚本 

openresty_service.sh

```shell
#!/bin/bash

##帮助文档
openresty_service_help() {
  echo "input command parameter: {start | stop | reload }"
}


### Nginx服务开启 #################################################################
openresty_start() {
  openresty -p /opt/openresty/
  echo "openresty service starting..."
}



### Nginx服务停止 #################################################################
openresty_stop() {
  openresty -p /opt/openresty/ -s stop
  echo "openresty service stoping..."
}


### Nginx服务重启 #################################################################
openresty_reload() {
  openresty -p /opt/openresty/ -s reload
  echo "openresty service reloading..."
}


### main #################################################################

main() {
  #echo "Welcome to Openresty services"

  case "${1:-}" in

    start)
      openresty_start
      ;;

    stop)
      openresty_stop
      ;;

    reload)
      openresty_reload
      ;;


    help)
      openresty_service_help
      ;;

    *)
      openresty_service_help
      ;;
  esac
}


main "$@"

```

### 1.2 模拟访问脚本

hkData.sh

```shell
#!/bin/bash
##这里的nginx端口要和上面配置的一致
url=http://localhost:8802/data/userlogs
sleep=5000
begin=`date -d "0 day" +"%Y%m%d"`
end=`date -d "+1 day" +"%Y%m%d"`

java -jar /opt/openresty/bin/logData.jar  \
-- --url $url   \
--sleep $sleep \
-- --begin $begin \
-- --end $end

```

### 1.3 用户日志生产脚本

userlog.sh

```shell
#!/bin/bash

url=localhost:8802/data/userlogs
sleep=10

java -jar /opt/openresty/bin/logData.jar \
--url $url \
--sleep $sleep

```



### 1.4 用户日志服务脚本

userlog_service.sh

```shell
#!/bin/bash


##帮助文档
ulogs_service_help() {
  echo "input command parameter: {start | stop | restart }"
}


### 服务开启：用户行为日志数据模拟制造 服务  #################################################################

##服务开启
ulogs_start() {
  /opt/openresty/bin/userlog.sh
  echo "ulogs service starting..."
}



### 服务停止：用户行为日志数据模拟制造 服务 #################################################################

##服务停止
ulogs_stop() {
  ps -ef | grep 'hkData*' | grep -v grep | awk '{print $2}'| xargs kill -9 
  echo "ulogs service stoping..."
}



### main #################################################################

main() {
  #echo "Welcome to data services"

  case "${1:-}" in

    start)
      ulogs_start
      ;;

    stop)
      ulogs_stop
      ;;

    restart)
      ulogs_stop
      ulogs_start
      ;;


    help)
      ulogs_service_help
      ;;

    *)
      ulogs_service_help
      ;;
  esac
}


main "$@"

```

### 1.5 脚本赋权

说明：给/opt/openresty/bin目录下的每个.sh文件赋予执行权限，示例如下：

 ```
chmod +x openresty_service.sh
 ```



## 2 制造数据的java程序   

logData.jar

![image-20220420031021801](imges/image-20220420031021801.png)

将maven项目hkDate打包后，上传到 /opt/openresty/bin路径下即可



## 3 数据生产

### 3.1 启动服务

进入/opt/openresty/bin目录，执行如下命令：

```
./hkData.sh start
```

或通过服务脚本

```shell
./userlog_service.sh start
```

注意：在生产数据前，请启动openresty服务



启动脚本后效果：

![image-20220420031939108](imges/image-20220420031939108.png)

### 3.2 查看数据

生成的数据在/opt/openresty/bin/logs/hkdata.log文件中

![image-20220420032150360](imges/image-20220420032150360.png)



通过如下命令查看数据生成效果：

```
tail -f hkdata.log
```

![image-20220420032306214](imges/image-20220420032306214.png)

会发现屏幕数据在不断的滚动



### 3.3 收集数据

日志信息收集在/opt/data/目录下的ulogs文件

可以通过如下命令查看该文件数据变化

```
tail -f ulogs
```

![image-20220420032601755](imges/image-20220420032601755.png)

## 4 多服务器生产数据

### 4.1 软件安装和资料分发

在s20和s30上安装好openresty,将m10上/opt/openresty文件夹通过xsync分发到s20和s30，按照m10的流程生产数据



### 4.2 数据收集脚本

```
#!/bin/bash

##集群节点数量(节点名称规则：bigdata2sparkX,X为机器编号,如01,02,03等)
nodeNums=1


##帮助文档
ulogs_service_help() {
  echo "input command parameter: {start | stop | restart }"
}


### 服务开启：用户行为日志数据模拟制造 服务  #################################################################

##服务开启
ulogs_start() {
  for((i=1; i<=nodeNums; i++));
  do
	nodeName="bigdata2spark0"$i
	ssh root@$nodeName "/opt/openresty/bin/userlog.sh"
  done
  echo "ulogs service starting..."
}



### 服务停止：用户行为日志数据模拟制造 服务 #################################################################

##服务停止
ulogs_stop() {
  for((i=1; i<=nodeNums; i++));
  do
	nodeName="bigdata2spark0"$i
	ssh root@$nodeName "ps -ef | grep 'hkData*' | grep -v grep | awk '{print $2}'| xargs kill -9 "
  done
  echo "ulogs service stoping..."
}



### main #################################################################

main() {
  #echo "Welcome to data services"

  case "${1:-}" in

    start)
      ulogs_start
      ;;

    stop)
      ulogs_stop
      ;;

    restart)
      ulogs_stop
      ulogs_start
      ;;


    help)
      ulogs_service_help
      ;;

    *)
      ulogs_service_help
      ;;
  esac
}


main "$@"

```

